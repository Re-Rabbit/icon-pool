@require "styles/reset"
@require "styles/container"
@require "styles/typo"

*, *:before, *:after
  box bdbox

center-container(w)
  maw w
  mx a

a
  reset-theme()
  text-decoration none
  dis ib

/*
 * test.
 */
body
  reset-box()
  reset-font()
  reset-theme()
  reset-space()
  fz 16px
  lh 1.5
  
